# networktools
network stuff

Looking for tools that can provide very high throughput on trusted networks (encryption optional, but host/application authentication needed). The networks are 10G and the hosts are SSD drives.
